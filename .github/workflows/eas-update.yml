name: Legacy OTA Manual Only

on:
  workflow_dispatch:

jobs:
  notice:
    name: Automatic OTA moved
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Automatic production OTA is handled only by Publish Production OTA to prevent duplicate updates."
