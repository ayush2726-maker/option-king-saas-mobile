name: Biometric APK Paused

on:
  workflow_dispatch:

jobs:
  paused:
    name: Biometric build is paused
    runs-on: ubuntu-latest
    steps:
      - name: Paused notice
        run: echo "Biometric login will be added with the next native Expo APK build. Regular production OTA updates remain unchanged."
