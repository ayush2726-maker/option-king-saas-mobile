{
  "release": "daily-backtest-async-v3-force-publish",
  "channel": "production",
  "runtime": "1.0.1",
  "requested_at": "2026-08-21T11:20:00+05:30",
  "reason": "Force publish the current main App.js that now contains OKAI-DAILY-BACKTEST-ASYNC-V3. The previous OTA was published from the script commit before App.js itself was patched.",
  "validation": {
    "required_checks": [
      "App.js contains OKAI-DAILY-BACKTEST-ASYNC-V3",
      "Daily uses /backtest/daily/start instead of /backtest/run",
      "Daily polls /backtest/daily/status/{job_id}",
      "Monthly flow remains unchanged",
      "Android Metro export succeeds",
      "Production and default channels both publish successfully"
    ],
    "ui_fix": "Daily Backtest must run through background job polling and stay in-app.",
    "logic_safety": "No live trade entry, exit, SL, quantity, broker, AI, or order logic changes."
  },
  "trade_safety": "No trading logic changes."
}
