{
  "release": "multi-trade-install-order-v4",
  "channel": "production",
  "runtime": "1.0.1",
  "requested_at": "2026-08-04T10:32:00+05:30",
  "reason": "The V3 multi-trade runtime was installed after AppTradeExplanationPatched loaded App.js. App.js had already captured the original jsx/jsxs functions, so the legacy single Active Paper Trade card remained visible and no per-trade exit buttons rendered. Install the V3 JSX hook before the app module loads.",
  "validation": {
    "required_checks": [
      "DirectActiveTradeCardV3 installs before AppTradeExplanationPatched",
      "Two simultaneous OPEN trades are normalized and rendered separately",
      "Each exit request sends only the pressed trade_id",
      "Legacy floating EXIT TRADE NOW remains suppressed",
      "JavaScript syntax",
      "Android Metro export",
      "Existing mobile validation suite"
    ],
    "root_cause": "Runtime installation order: patched jsx/jsxs exports were attached only after TradeTab had captured unpatched local references.",
    "ui_fix": "Move installDirectActiveTradeCardV3 before requiring AppTradeExplanationPatched, while keeping the floating-overlay suppression guard after app load.",
    "data_behavior": "Open trades continue to merge from trade history, trade-live and bot signal payloads every three seconds."
  },
  "trade_safety": "Trade-tab display and explicit user-requested manual exit targeting only. No automatic signal, score, strategy, entry, exit, quantity, stop-loss, cooldown, risk, broker or order rule changes."
}
