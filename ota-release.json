{
  "release": "backtest-stability-v1",
  "channel": "production",
  "runtime": "1.0.1",
  "requested_at": "2026-08-21T09:12:00+05:30",
  "reason": "Force Publish Production OTA from current main so the persisted Backtest stability fix is bundled and delivered.",
  "validation": {
    "required_checks": [
      "App.js contains the Backtest stability fix",
      "OTA banner must not auto-reload the app in the background",
      "Large backtest trade lists are capped for rendering while summary totals remain complete",
      "Android Metro export succeeds",
      "Production and default channels both point to production"
    ],
    "ui_fix": "Backtest should remain in-app instead of being disrupted by an automatic OTA reload or excessive result rendering.",
    "logic_safety": "No trade entry, exit, SL, quantity, broker, AI, or order logic changes in this mobile OTA trigger."
  },
  "trade_safety": "No trading logic changes."
}
