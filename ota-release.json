{
  "release": "admin-user-pnl-native-v1",
  "channel": "production",
  "runtime": "1.0.1",
  "requested_at": "2026-08-18T10:20:00+05:30",
  "reason": "Show the secure all-user Paper/Live P&L report directly inside the native Admin and Account screens.",
  "validation": {
    "required_checks": [
      "Admin-only endpoint /admin/users/pnl is used with the signed-in bearer token",
      "Today, current open, all-time, Paper and Live net P&L remain separate",
      "Every registered user is shown with user-wise totals",
      "Unpriced open gateway positions are labelled as awaiting quote",
      "P&L card renders in both Admin and Account screens",
      "Production and legacy default channels both point to the production branch",
      "Non-admin users cannot see or load the admin P&L card",
      "Android Metro export succeeds"
    ],
    "ui_fix": "Add a native All Users P&L card so the admin no longer needs the browser panel.",
    "logic_safety": "Read-only admin presentation change. Entry, exit, SL, quantity, broker, AI and order logic are unchanged."
  },
  "trade_safety": "No entry, exit, SL, quantity, broker or order logic changes."
}
