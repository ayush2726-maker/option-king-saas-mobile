{
  "release": "restart-loop-recovery-v5",
  "channel": "production",
  "runtime": "1.0.1",
  "requested_at": "2026-09-04T21:56:00+05:30",
  "reason": "Retry the production recovery bundle after fixing the stale V2 OTA guard regression check. In-app OTA polling stays disabled and persistent auth stays enabled for Rohit/Rakesh reload-loop recovery.",
  "validation": {
    "required_checks": [
      "OtaStatusBanner contains no Expo update polling or reload calls",
      "Persistent auth restores dashboard from saved token",
      "Transient /auth/me failure does not force logout",
      "Android export succeeds",
      "Production and default OTA channels publish successfully"
    ],
    "ui_fix": "OKAI-INAPP-OTA-DISABLED-V3 + OKAI-PERSISTENT-AUTH-SESSION-V1",
    "logic_safety": "No broker execution, quantity, SL, exit, AI scoring, or trading-strategy logic changes."
  },
  "trade_safety": "App lifecycle/auth stability only; trading logic unchanged."
}
